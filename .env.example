# Database
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=__duson

# Media base URL (must end with /)
BASE_URL=https://duson.am/storage/media/

# Where to save item_id folders (default repo root)
OUTPUT_DIR=.

# Network
REQUEST_TIMEOUT=10
