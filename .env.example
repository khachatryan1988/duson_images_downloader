# Database
DB_HOST=0.0.0.0
DB_USER=
DB_PASSWORD=
DB_NAME=

# Media base URL (must end with /)
BASE_URL=

# Where to save item_id folders (default repo root)
OUTPUT_DIR=.

# Network
REQUEST_TIMEOUT=10
